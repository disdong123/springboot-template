spring.config.activate.on-profile: test
spring:
  data:
    redis:
      host: localhost
      port: 6377

---
spring.config.activate.on-profile: default
spring:
  data:
    redis:
      host: localhost
      port: 6379